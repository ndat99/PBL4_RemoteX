<Window x:Class="RemoteX.Server.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RemoteX Server" Height="583" Width="441"
        MinHeight="400" MinWidth="350"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize" WindowStartupLocation="Manual" Icon="Images/RemoteX.ico">

    <Window.Resources>
        <!-- Styles -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4A90E2"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Margin" Value="0,5,0,2"/>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4CAF50"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5BA0F2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3A80D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StopButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#F44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F66356"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClearButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#FF9800"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFB74D"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#F57C00"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClientItemStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="TitleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter x:Name="contentPresenter" HorizontalAlignment="Center" VerticalAlignment="Center"
                                Content="{TemplateBinding Content}" TextElement.Foreground="{TemplateBinding Foreground}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground"  Value="#4A90E2"/>
                                <Setter TargetName="contentPresenter" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#4A90E2" BlurRadius="8" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="#4A90E2"/>
                                <Setter TargetName="contentPresenter" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#4A90E2" BlurRadius="12" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" BorderBrush="#FF4A90E2" BorderThickness="2" Background="#FF1E1E1E">
        <DockPanel>
            <!-- Title Bar - Fixed at top -->
            <Grid x:Name="titleBar" DockPanel.Dock="Top" MouseLeftButtonDown="titleBar_MouseLeftButtonDown" 
                  Height="44" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon and Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                    <Ellipse Width="20" Height="20" Margin="0,0,8,0">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="Images/RemoteX.png"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock Text="RemoteX - Server" FontWeight="SemiBold" Foreground="White" FontSize="14"/>
                </StackPanel>

                <!-- Window Controls -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Width="90">
                    <Button x:Name="btnMinimize" Content="−" Width="40" Height="40" Background="Transparent" Style="{StaticResource TitleButtonStyle}" 
                        Foreground="White" BorderThickness="0" FontSize="20" Click="btnMinimize_Click" Cursor="Hand"/>
                    <Button x:Name="btnClose" Content="⏻" Width="40" Height="40" Background="Transparent" Style="{StaticResource TitleButtonStyle}"
                        Foreground="White" BorderThickness="0" FontSize="20" Click="btnClose_Click" Cursor="Hand"/>
                </StackPanel>
            </Grid>

            <!-- Footer - Fixed at bottom -->
            <Border DockPanel.Dock="Bottom" Height="30" Background="Transparent">
                <TextBlock Text="Nguyen Hoang Phuc - Nguyen Dang Anh Tien" 
                           Foreground="#4A90E2" FontSize="10" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Main Content - Fills remaining space -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          Padding="20,10,20,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*" MinHeight="150"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Server Status -->
                    <Border Grid.Row="0" Background="#252526" CornerRadius="8" BorderBrush="#FF454545" 
                            BorderThickness="1" Padding="20,15" Margin="0,0,0,15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Server Icon and Status -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <Ellipse Width="24" Height="24" Margin="0,0,20,0">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="/Views/Images/Status.png" Stretch="Fill"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" 
                                           Style="{StaticResource StatusTextStyle}"/>
                            </StackPanel>

                            <!-- Server Indicator -->
                            <Ellipse Grid.Column="2" Width="12" Height="12" x:Name="statusIndicator" 
                                     Fill="{Binding StatusDot}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Separator -->
                    <Border Grid.Row="1" Height="1" Background="#FF454545" Margin="0,0,0,15"/>

                    <!-- Connected Clients Section -->
                    <Border Grid.Row="2" Background="#252526" CornerRadius="8" BorderBrush="#FF454545" 
                            BorderThickness="1" Padding="20" MinHeight="150">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Header -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                                <Ellipse Width="24" Height="24" Margin="0,0,10,0">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="/Views/Images/Client.png"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock Text="Máy khách đã kết nối:" Style="{StaticResource HeaderTextStyle}"/>
                                <TextBlock x:Name="txtClientCount" Text="(*)" Foreground="#4A90E2" FontSize="14" 
                                           VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </StackPanel>

                            <!-- Client List -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                                          HorizontalScrollBarVisibility="Disabled" Background="Transparent">
                                <ItemsControl ItemsSource="{Binding Clients}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource ClientItemStyle}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Chấm trạng thái xanh -->
                                                    <Ellipse Grid.Column="0" Width="8" Height="8" Fill="#4CAF50"
                                                            VerticalAlignment="Center" Margin="0,0,10,0"/>

                                                    <!-- ID + Password -->
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding MachineName, StringFormat='Thiết bị: {0}'}"
                                                                  Foreground="#AAAAAA" FontSize="11" Margin="0,2,0,0"
                                                                  TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding Id}" 
                                                                  Foreground="White" FontWeight="SemiBold" FontSize="13"
                                                                  TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Text="{Binding Password, StringFormat='Mật khẩu: {0}'}" 
                                                                  Foreground="#CCCCCC" FontSize="11" Margin="0,2,0,0"
                                                                  TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- Separator -->
                    <Border Grid.Row="3" Height="1" Background="#FF454545" Margin="0,15"/>

                    <!-- Control Buttons -->
                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <Button x:Name="btnStart" Content="Khởi động" Style="{StaticResource ButtonStyle}" Click="btnStart_Click" Command="{Binding StartCommand}"/>
                        <Button x:Name="btnStop" Content="Dừng lại" Style="{StaticResource StopButtonStyle}" Click="btnStop_Click" Command="{Binding StopCommand}"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>