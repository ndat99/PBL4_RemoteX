<Window x:Class="RemoteX.Server.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RemoteX Server" Height="500" Width="441"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize" WindowStartupLocation="CenterScreen" Icon="Images/RemoteX.png">

    <Window.Resources>
        <!-- Styles -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4A90E2"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Margin" Value="0,5,0,2"/>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4CAF50"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5BA0F2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3A80D2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StopButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#F44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F66356"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClearButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#FF9800"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFB74D"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#F57C00"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClientItemStyle" TargetType="Border">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" BorderBrush="#FF4A90E2" BorderThickness="2" Background="#FF1E1E1E">
        <Grid>
            <!-- Title Bar -->
            <Grid x:Name="titleBar" Height="40" VerticalAlignment="Top" MouseLeftButtonDown="titleBar_MouseLeftButtonDown" Margin="0,1,0,0" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon and Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                    <Ellipse Width="20" Height="20" Margin="0,0,8,0">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="Images/RemoteX.png"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock Text="RemoteX - Server" FontWeight="SemiBold" Foreground="White" FontSize="14"/>
                </StackPanel>

                <!-- Window Controls -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="btnMinimize" Content="−" Width="40" Height="40" Background="Transparent" 
                        Foreground="White" BorderThickness="0" FontSize="16" Click="btnMinimize_Click"/>
                    <Button x:Name="btnMaximize" Content="□" Width="40" Height="40" Background="Transparent" 
                        Foreground="White" BorderThickness="0" FontSize="12" Click="btnMaximize_Click"/>
                    <Button x:Name="btnClose" Content="×" Width="40" Height="40" Background="Transparent" 
                        Foreground="White" BorderThickness="0" FontSize="16" Click="btnClose_Click"/>
                </StackPanel>
            </Grid>

            <!-- Main Content -->
            <Grid Margin="20,50,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Server Status -->
                <Border Grid.Row="0" Background="#252526" CornerRadius="8" BorderBrush="#FF454545" BorderThickness="1" Padding="20,15" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Server Icon and Status -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Ellipse Width="24" Height="24" Margin="0,0,10,0" Fill="White"/>
                            <TextBlock x:Name="txtStatus" Text="Đã tắt Server" Style="{StaticResource StatusTextStyle}"/>
                        </StackPanel>

                        <!-- Server Indicator -->
                        <Ellipse Grid.Column="2" Width="12" Height="12" x:Name="statusIndicator" Fill="#4CAF50" 
                                 VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Separator -->
                <Border Grid.Row="1" Height="1" Background="#FF454545" Margin="0,0,0,15"/>

                <!-- Connected Clients Section -->
                <Border Grid.Row="2" Background="#252526" CornerRadius="8" BorderBrush="#FF454545" BorderThickness="1" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                            <Ellipse Width="24" Height="24" Margin="0,0,10,0" Fill="White"/>
                            <TextBlock Text="Máy khách đã kết nối:" Style="{StaticResource HeaderTextStyle}"/>
                            <TextBlock x:Name="txtClientCount" Text="(2)" Foreground="#4A90E2" FontSize="14" 
                                       VerticalAlignment="Center" Margin="5,0,0,0"/>
                        </StackPanel>

                        <!-- Client List -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Background="Transparent">
                            <ItemsControl ItemsSource="{Binding Clients}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource ClientItemStyle}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Chấm trạng thái xanh -->
                                                <Ellipse Grid.Column="0" Width="8" Height="8" Fill="#4CAF50"
                                 VerticalAlignment="Center" Margin="0,0,10,0"/>

                                                <!-- ID + Password -->
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Id}" 
                                       Foreground="White" FontWeight="SemiBold" FontSize="13"/>
                                                    <TextBlock Text="{Binding Password, StringFormat='Mật khẩu: {0}'}" 
                                       Foreground="#CCCCCC" FontSize="11" Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- Separator -->
                <Border Grid.Row="3" Height="1" Background="#FF454545" Margin="0,15"/>

                <!-- Control Buttons -->
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button x:Name="btnStart" Content="Khởi động" Style="{StaticResource ButtonStyle}" Click="btnStart_Click"/>
                    <Button x:Name="btnStop" Content="Dừng lại" Style="{StaticResource StopButtonStyle}" Click="btnStop_Click"/>
                    <Button x:Name="btnClearLog" Content="Xóa nhật ký" Style="{StaticResource ClearButtonStyle}"/>
                </StackPanel>
            </Grid>

            <!-- Footer -->
            <Grid Height="30" VerticalAlignment="Bottom">
                <TextBlock Text="RemoteX Server v1.0" 
                       Foreground="#4A90E2" FontSize="10" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>